# Pc-checking-tool-R6
A pc checking tool built with python and rust. You put in what you want it to flag and build the exe. The target pc runs the .exe, It will ask for consent and a bunch of other things like what to scan. It scans, Browsing history, discord chats (Half working), file system, and the registry editor.
